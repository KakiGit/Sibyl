# Sibyl Development Agents

## Notes

* **ALWAYS** make a git commit after completing a feature.
* **DO NOT** push the change.
* Run the server and Web UI locally without using docker/podman.
* Test WebUI with playwright MCP
* Create readable test for the server and Web UI. As the project owner will only check tests to evaluate the features.
* Everytime after the user gives an input, use memory_recall tool with the user input to fetch relevant memory

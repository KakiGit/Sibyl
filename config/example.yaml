harness:
  default: opencode
  opencode:
    url: http://localhost:8080
    model: claude-3-sonnet

memory:
  backend: falkordb
  host: localhost
  port: 6379
  database: sibyl_memory

ipc:
  socket_path: /tmp/sibyl-ipc.sock

logging:
  level: info
  file: sibyl.log